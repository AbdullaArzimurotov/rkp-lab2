## Цель работы
Изучить способ использования репозитория в Spring Data JPA на примере базы данных H2.

## Архитектура проекта
- **Стек:** Spring Boot 3, Maven, Java 17, Spring Data JPA, H2 Database, Thymeleaf
- **Сущность (Вариант 2):** `Schoolboy` (Школьник)
- **Поля:** ID, Фамилия, Имя, Отчество, Пол, Национальность, Рост, Вес, Дата рождения, Номер телефона, Домашний телефон.

## Алгоритм работы
1. В `application.properties` настроено подключение к in-memory базе данных H2.
2. Создан класс `@Entity` (`Schoolboy`) с необходимыми полями и аннотациями JPA.
3. Создан интерфейс `SchoolboyRepository`, наследующийся от `CrudRepository` для выполнения базовых операций (CRUD).
4. Написан `SchoolboyController`, который обрабатывает GET и POST запросы, взаимодействует с репозиторием и возвращает HTML-шаблоны.
5. Интерфейс реализован с использованием Thymeleaf (страницы добавления, редактирования, просмотра деталей и списка).
6. В файл `data.sql` добавлены первоначальные скрипты для автоматического заполнения базы данных при запуске.

## Скриншоты работы приложения

**Список школьников:**
![Список](screenshot1.png)

**Форма добавления/редактирования:**
![Форма](screenshot2.png)